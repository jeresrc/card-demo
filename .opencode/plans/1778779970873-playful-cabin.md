# Plan: Single-card demo page

## Goal
Replace the entire current home page with one demo card matching the provided design and SVG. Remove all other starter UI/chrome (header, footer, devtools, about route, theme toggle, etc.).

## Card spec (from image + SVG)
- Background: solid purple `#694C9A`.
- Shape: rounded rectangle ~`24px` radius. Aspect roughly portrait card (`~660 x 840` at max), responsive (max width clamp, full width on mobile with padding).
- Slight 3D tilt for the demo look: subtle `rotate(-2deg)` + soft drop shadow. Optional - confirmed acceptable.
- Top-left text block:
  - `AMAR 100` - uppercase, tracked, semi-transparent white (`rgba(255,255,255,0.55)`), small/medium weight.
  - `Josefina Staudenmaier` - large display, white, bold, two-line break (`Josefina` / `Staudenmaier`).
- Bottom-left: `#001` in same muted white as kicker.
- Bottom-right illustration: the provided SVG (heart + cat silhouette) anchored to the bottom-right, large (`~55%` of card width), partially clipped by the card via `overflow-hidden`. SVG already has `opacity="0.4"` baked in - keep as-is.
- Light text color across card; no dark mode variant needed for the demo.

## Files to modify

### 1. `src/routes/__root.tsx`
- Remove imports/usages: `Header`, `Footer`, `TanStackDevtools`, `TanStackRouterDevtoolsPanel`, `THEME_INIT_SCRIPT`.
- Keep: `HeadContent`, `Scripts`, stylesheet link.
- Update `<title>` to `AMAR 100`.
- Body: just render `{children}` inside a minimal wrapper. Drop `selection:bg-[...]` styling (optional).

### 2. `src/routes/index.tsx`
Replace entire content. New layout:
- `<main>` centers content (`min-h-screen flex items-center justify-center p-6`).
- Card container: `relative overflow-hidden rounded-3xl bg-[#694C9A] text-white shadow-2xl` with explicit max-width (`max-w-[560px]`) and portrait aspect via padding (`aspect-[4/5]` or fixed `min-h` ~ 760px).
- Inner padding ~`p-10`.
- Layout columns: flex column with `justify-between` so kicker/heading sit top, `#001` sits bottom-left.
- Decorative SVG: absolutely positioned `bottom-0 right-0` (with small negative offset to bleed off edge), inline JSX from the provided SVG (kept verbatim incl. `opacity="0.4"`, filters, gradients). Use `pointer-events-none`.
- Optional slight tilt: wrap card in container with `transform rotate-[-2deg]`.

### 3. `src/styles.css`
- Keep `@import "tailwindcss"`.
- Remove starter custom CSS (theme tokens, hero gradients, `.island-shell`, `.feature-card`, `.island-kicker`, `.nav-link`, `.site-footer`, `.rise-in`, body backgrounds, `code` styles, Fraunces font import).
- Keep Manrope import + `@theme { --font-sans }`.
- Add minimal background color for page (e.g. `body { background: #f4f4f5; }` or similar neutral so card pops).

### 4. Delete starter files
- `src/routes/about.tsx`
- `src/components/Header.tsx`
- `src/components/Footer.tsx`
- `src/components/ThemeToggle.tsx`

### 5. `src/routeTree.gen.ts`
- Will be auto-regenerated by TanStack Router plugin on `pnpm dev` / `pnpm build`. No manual edit. If stale references to `/about` cause type errors, run `pnpm build` once to regenerate.

## Verification
1. `pnpm dev` → open `http://localhost:3000`.
2. Confirm: solid neutral background, single purple card centered, `AMAR 100` kicker top-left, `Josefina` / `Staudenmaier` heading, `#001` bottom-left, translucent heart/cat illustration bottom-right clipped by card edge.
3. No header, no footer, no devtools panel, no `/about` link visible.
4. Responsive: card scales down on narrow widths, illustration stays anchored bottom-right.
5. `pnpm lint` and `pnpm build` clean.
